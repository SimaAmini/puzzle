import { Text } from '../../components';
import Screen from '../../layout/screen';

export const Settings = () => {
  return (
    <Screen>
      <Text>Settings</Text>
      <Text>logout</Text>
    </Screen>
  );
};
